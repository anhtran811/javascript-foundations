function createDragon(name) {
  return {
    name: name,
  };
}

// arrow function: 

// const createDragon = (name) => (
//     { name }
//   )
  

module.exports = {
  createDragon, 
  // greetRider, 
  // eat, 
  // findFireBreathers
}